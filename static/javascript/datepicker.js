//start with defining some constants


const options = {
    format: 'dd-mm-yyyy',
    disablePast: true
}
const myDatepicker = new mdb.Datepicker(document.getElementById('datepicker-disable-past'), options)
